# Python — Web e APIs

Estude HTTP, requests, responses, métodos, headers, status codes, JSON, cookies, sessões, REST, validação, autenticação e autorização.

Segurança: validação, gestão de segredos, queries parametrizadas, rate limiting, logs e menor privilégio.

Projeto: API local de biblioteca com utilizadores, livros, empréstimos, pesquisa, persistência, autenticação, autorização, testes e documentação. Use apenas ambientes controlados ou autorizados.