# Dia 027 — SQL com Python

## Conteúdo
`SELECT`, `INSERT`, `UPDATE`, `DELETE`, parâmetros, `commit` e tratamento de erros.

## Desafio
Migrar a persistência JSON do projeto de tarefas para SQLite sem alterar a interface pública do sistema.
