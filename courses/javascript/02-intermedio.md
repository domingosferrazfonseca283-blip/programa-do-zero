# JavaScript/TypeScript — Volume 2: Intermediário

Escopo e closures; arrays; objetos e protótipos; classes; módulos; Promises; async/await; erros; TypeScript; interfaces e generics; testes; HTTP; persistência.

## Projeto
API local de tarefas com validação, testes, persistência, autenticação de laboratório e documentação.
